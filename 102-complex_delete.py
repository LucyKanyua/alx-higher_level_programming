#!/usr/bin/python3
def complex_delete(a_dictionary, value):
    list_keys = list(a_ditionary keys())

    for value_dic in list_keys:
        if value == a_dictionary.get(value_dic):
            del a_ditionary[value_di]

            return (a_ditionary)
